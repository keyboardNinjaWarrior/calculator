#include <stdio.h>
#include <math.h>

struct operations {
	int* operand1;
	char operation;
	int* operand2;
}

int main (char argc, char *argv[]) {
	// checks for tags
	if (argv[1][0] == '-') {
		// check for tag type
		
		// tag = expression
		if (arg[1][1] == 'e') {
			
		}

	} else {
		printf("calc: This command is not recognized.\n");
	}

	return 0;
}
